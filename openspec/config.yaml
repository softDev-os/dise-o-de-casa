# SDD Configuration — diseño-de-casa
# Generated: 2026-05-28

sdd:
  execution_mode: interactive
  artifact_store: openspec
  chained_pr_strategy: auto-forecast
  review_budget_lines: 400

project:
  name: diseño-de-casa
  description: >
    Sistema de cotizaciones profesionales para
    construcción y renovación de viviendas
  language: es
  type: html-standalone

# Strict TDD (enable when test infrastructure exists)
tdd:
  enabled: false
  # test_command: "npm test"
  # coverage_threshold: 80

# Project context
context:
  domain: construction-quoting
  tech_stack:
    - HTML5
    - CSS3 (custom properties)
    - JavaScript (vanilla)
    - SVG (interactive floor plans)
  assets:
    - cotizacion_apartamento_v1.html
    - cotizacion_apartamento_v2.html
    - cotizacion_bano_v3.html
    - "site photos (JPG)"
    - floor_plan.png
